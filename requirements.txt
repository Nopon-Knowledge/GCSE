# Runtime dependencies for ImageNet classification and COCO detection.
# Keep these constraints aligned with pyproject.toml (core + coco).
numpy>=1.24
pillow>=9
torch>=2.1,<3
torchvision>=0.16,<1
pycocotools>=2.0.7
